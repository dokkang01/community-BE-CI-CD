spring:
  datasource:
    url: jdbc:mysql://community.cpwk6q8qg95v.ap-northeast-2.rds.amazonaws.com:3306/communityservice?serverTimezone=UTC&useSSL=false
    username: admin
    password: Gkswldn0814!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create   # 개발 중에는 create / create-drop / update 중 선택
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

app:
  s3:
    bucket: express-backend-roy
    region: ap-northeast-2

  profiles:
    active: dev

logging:
  level:
    org.hibernate.SQL: debug